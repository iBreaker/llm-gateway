# LLM Gateway 环境配置示例
# 只保留数据库连接信息，其他配置存储在数据库中

# PostgreSQL 数据库连接
DATABASE_URL="postgresql://username:password@localhost:5432/llm_gateway"

# Redis 连接 (可选，如果不使用请留空)
REDIS_URL="redis://localhost:6379"

# 运行环境 (development/production)
NODE_ENV="development"