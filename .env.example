# LLM Gateway 环境配置示例
# Vercel 部署时会自动设置数据库环境变量，本地开发需要手动配置

# 本地开发数据库连接 (Vercel 部署时自动提供 POSTGRES_URL)
POSTGRES_URL="postgresql://username:password@localhost:5432/llm_gateway"
POSTGRES_URL_NON_POOLING="postgresql://username:password@localhost:5432/llm_gateway"

# 运行环境 (development/production)
NODE_ENV="development"

# 注意：
# - Vercel 部署时，POSTGRES_URL 和 POSTGRES_URL_NON_POOLING 会自动设置
# - 本地开发时，需要手动配置上述环境变量
# - 其他配置项存储在数据库中，不需要环境变量